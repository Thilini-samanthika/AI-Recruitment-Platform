server:
  port: 8082

spring:
  application:
    name: candidate-service
  data:
    mongodb:
      uri: mongodb://localhost:27018/candidate_test_db
      auto-index-creation: true

security:
  api-key: candidate-service-secret-key-12345

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
