# Frontend Dockerfile (Member 5)
FROM node:20-alpine
WORKDIR /app
# Placeholder / Stub container for Member 5 Frontend development
CMD ["sh", "-c", "echo 'Frontend container starting on port 3000...' && while true; do sleep 3600; done"]
